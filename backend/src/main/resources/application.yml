spring:
  application:
    name: BoardGames
  datasource:
    url: jdbc:h2:file:C:/tmp/h2/boardgames
    username: sa
    password: pra
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true